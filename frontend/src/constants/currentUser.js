/**
 * Mock authenticated user used across the frontend while auth is out of scope for EX2.
 * This ID is sent to backend appointment endpoints for ownership checks.
 */
export const CURRENT_USER = {
  id: 1,
  name: 'Henya',
  email: 'henya@example.com',
}
