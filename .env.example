# Copy to `.env` in the repository root for local runs and Docker Compose.

# Groq (primary AI — Llama 3.1 on Groq)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Google Gemini (automatic failover when Groq is unavailable)
GEMINI_API_KEY=
